#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include "sort.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	char buff[3];
	cocktail_sort_list(NULL);
	read(2, buff, sizeof(buff));
	printf("%s\n", buff);
	return (0);
}
